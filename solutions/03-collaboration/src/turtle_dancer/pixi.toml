# The package manifest. Name, version, description, maintainer and dependencies
# all come out of package.xml, which stays the single source of truth.

[package.build.backend]
channels = ["conda-forge"]
name = "pixi-build-ros"
version = "*"

[package.build.config]
distro = "jazzy"
